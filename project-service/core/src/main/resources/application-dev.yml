server:
    port: 8800
spring:
    datasource:
        url: jdbc:mysql://127.0.0.1:3306/hawk?useUnicode=true&characterEncoding=UTF-8&serverTimezone=GMT%2B8
        username: root
        password: 123456
eureka:
    instance:
        hostname: localhost
    client:
        serviceUrl:
            defaultZone: http://localhost:8080/eureka/
hawk:
    project:
        codebase:
            git:
                repoPath: /tmp/repo
                defaultPassword: git-admin-password
                defaultUsername: git-admin-username
